services:
  docs:
    image: foxreviews_local_docs
    container_name: foxreviews_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./foxreviews:/app/foxreviews:z
    ports:
      - '9000:9000'
    command: /start-docs
